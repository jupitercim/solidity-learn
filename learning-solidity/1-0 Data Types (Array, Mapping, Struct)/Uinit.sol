pragma solidity ^0.8.0;

contract DataTypes {
    // uint8 can represent values from 0 to 255 (2^8 - 1),
    //while uint256 can represent values from 0 to 2^256 - 1.

    uint public ss = 122;

    uint8 public sss = 1; // (2^8 - 1),
    uint8 public ssss = 1256;


    //comes in various sizes: uint8, uint16, uint32, uint64, uint128, uint256

}
